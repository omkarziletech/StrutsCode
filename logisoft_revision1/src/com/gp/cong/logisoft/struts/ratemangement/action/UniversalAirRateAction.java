/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package com.gp.cong.logisoft.struts.ratemangement.action;

import java.util.List;
import java.util.ArrayList;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

import org.apache.struts.action.Action;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;

import com.gp.cong.logisoft.domain.GenericCode;
import com.gp.cong.logisoft.domain.UniverseAirFreight;
import com.gp.cong.logisoft.hibernate.dao.GenericCodeDAO;
import com.gp.cong.logisoft.struts.ratemangement.form.UniversalAirRateForm;

/** 
 * MyEclipse Struts
 * Creation date: 07-30-2008
 * 
 * XDoclet definition:
 * @struts.action path="/universalAirRate" name="universalAirRateForm" input="jsps/ratemanagement/universalAirRate.jsp" scope="request"
 */
public class UniversalAirRateAction extends Action {

    public ActionForward execute(ActionMapping mapping, ActionForm form,
            HttpServletRequest request, HttpServletResponse response) throws Exception {
        UniversalAirRateForm universalAirRateForm = (UniversalAirRateForm) form;// TODO Auto-generated method stub
        String amount = universalAirRateForm.getAmount();
        String button = universalAirRateForm.getButtonValue();
        String range = universalAirRateForm.getStartrange();
        HttpSession session = ((HttpServletRequest) request).getSession();
        String forwardName = null;
        GenericCode genObj = new GenericCode();
        UniverseAirFreight universeAirFreight = new UniverseAirFreight();
        GenericCodeDAO genericCodeDAO = new GenericCodeDAO();
        List uniAirFright = null;
        List reoveList = new ArrayList();
        if (request.getParameter("ind") != null) {
            UniverseAirFreight airStandard = new UniverseAirFreight();
            int ind = Integer.parseInt(request.getParameter("ind"));
            List codeList1 = (List) session.getAttribute("uniarifrightlist");
            if (codeList1 != null) {
                airStandard = (UniverseAirFreight) codeList1.get(ind);
            }
            session.setAttribute("edituniarifright", airStandard);
            forwardName = "uniairEdit";

        } else {
            if (range != null && !range.equals("0")) {
                genObj = (GenericCode) genericCodeDAO.findById(Integer.parseInt(range));
                universeAirFreight.setWeightRange(genObj);




            }
            if (amount != null && !amount.equals("")) {
                universeAirFreight.setAmount(Double.parseDouble(amount));
            } else {
                universeAirFreight.setAmount(0.00);
            }
        }
        if (button != null && button.equals("add")) {
            if (session.getAttribute("uniarifrightlist") != null) {
                uniAirFright = (List) session.getAttribute("uniarifrightlist");
            } else {
                uniAirFright = new ArrayList();

            }
            uniAirFright.add(universeAirFreight);
            session.setAttribute("uniarifrightlist", uniAirFright);
            forwardName = "uniairAdd";
            if (session.getAttribute("univerAriFreight") != null) {
                session.removeAttribute("univerAriFreight");

            }
        }
        if (button != null && button.equals("addItem")) {
            session.setAttribute("univerAriFreight", "univerAriFreight");
            forwardName = "uniairAdd";
        }
        return mapping.findForward(forwardName);
    }
}