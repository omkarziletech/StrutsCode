/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package com.gp.cong.logisoft.struts.action;

import java.io.IOException;
import java.util.List;

import javax.jms.Session;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

import org.apache.struts.action.Action;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;

import com.gp.cong.logisoft.domain.CarriersOrLineTemp;
import com.gp.cong.logisoft.hibernate.dao.PortsDAO;
import com.gp.cong.logisoft.struts.form.SearchScheduleCodeForm;
import com.gp.cong.logisoft.domain.PortsTemp;

/** 
 * MyEclipse Struts
 * Creation date: 08-04-2008
 * 
 * XDoclet definition:
 * @struts.action path="/searchScheduleCode" name="searchScheduleCodeForm" input="/jsps/datareference/searchScheduleCode.jsp" scope="request" validate="true"
 * @struts.action-forward name="searchScheduleCode" path="/jsps/datareference/searchScheduleCode.jsp"
 */
public class SearchScheduleCodeAction extends Action {
	/*
	 * Generated Methods
	 */

	/** 
	 * Method execute
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return ActionForward
	 */
	public ActionForward execute(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) throws Exception {
		SearchScheduleCodeForm searchScheduleCodeForm = (SearchScheduleCodeForm) form;// TODO Auto-generated method stub
		HttpSession session = request.getSession();
		String scheduleCode = searchScheduleCodeForm.getScheduleCode();
		String city = searchScheduleCodeForm.getCity();
		
		String buttonValue = searchScheduleCodeForm.getButtonValue();
		String index = searchScheduleCodeForm.getIndex();
		PortsDAO portsDAO = new PortsDAO();
		PortsTemp scheduleobj = new PortsTemp();
		
		
		if((request.getParameter("index")!=null && !request.getParameter("index").equals("")) || (index!= null && !index.equals("")))
		{
			int ind = Integer.parseInt(request.getParameter("index"));
			if(index!= null)
			{
				ind =Integer.parseInt(index); 
			}
			List portslist = (List)session.getAttribute("portslist");
			scheduleobj = (PortsTemp)portslist.get(ind);
			session.setAttribute("scheduleobj", scheduleobj);
			request.setAttribute("checked", "checked");
			return mapping.findForward("searchScheduleCode");
		}
		else
		{
			List portslist = portsDAO.findForschcodeandcity(scheduleCode,city);
			session.setAttribute("portslist", portslist);
			if( ((List)session.getAttribute("portslist")).size() == 0 )
			  {
				  session.setAttribute("Notfound",scheduleCode );
				 
			  }
			
		}
			response.sendRedirect(request.getContextPath()+ "/jsps/datareference/searchScheduleCode.jsp");
		return null;
	}
}