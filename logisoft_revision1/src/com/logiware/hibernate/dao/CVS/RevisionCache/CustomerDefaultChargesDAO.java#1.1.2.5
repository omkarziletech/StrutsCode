package com.logiware.hibernate.dao;

import com.gp.cong.logisoft.hibernate.dao.BaseHibernateDAO;
import com.logiware.hibernate.domain.CustomerDefaultCharges;
import java.util.List;
import org.hibernate.Query;

/**
 * Data access object (DAO) for domain model class ApInvoice.
 * @see com.gp.cvst.logisoft.hibernate.dao.ApInvoice
 * @author MyEclipse - Hibernate Tools
 */
public class CustomerDefaultChargesDAO extends BaseHibernateDAO {
    // property constants

    /**
     * @param transientInstance
     */
    public void save(CustomerDefaultCharges customerDefaultCharges) throws Exception {
        getSession().save(customerDefaultCharges);
        getSession().flush();
    }

    /**
     * @param persistentInstance
     */
    public void delete(CustomerDefaultCharges customerDefaultCharges) throws Exception {
        getSession().delete(customerDefaultCharges);
        getSession().flush();
    }

    /**
     *
     * @param updateInstance
     */
    public void saveOrUpdate(CustomerDefaultCharges customerDefaultCharges) throws Exception {
        getSession().saveOrUpdate(customerDefaultCharges);
        getSession().flush();
    }

    /**
     * @param id
     * @return
     */
    public CustomerDefaultCharges findById(Integer id) throws Exception {
        CustomerDefaultCharges instance = (CustomerDefaultCharges) getSession().get("com.logiware.hibernate.domain.CustomerDefaultCharges", id);
        return instance;
    }

    public List findByProperty(String propertyName, Object value) throws Exception {
        String queryString = "from CustomerDefaultCharges as model where model." + propertyName + " = ?0";
        Query queryObject = getSession().createQuery(queryString);
        queryObject.setParameter("0", value);
        return queryObject.list();
    }
}
